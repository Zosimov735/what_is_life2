// Fixture: an escape marker with no reason suppresses nothing and is itself
// reported.

// lexicon-check: allow-term
export const creatureCharge = 1.0;

export function Panel() {
  // lexicon-check: allow-inline —
  return <p>Follow the bright current.</p>;
}
