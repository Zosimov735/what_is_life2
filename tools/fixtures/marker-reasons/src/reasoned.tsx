// Fixture: the same two markers, each given a reason, still suppress.

// lexicon-check: allow-term — fixture proving a reasoned marker still works
export const creatureIndex = 0;

export function Notice() {
  // lexicon-check: allow-inline — fixture proving a reasoned marker still works
  return <p>Follow the bright current.</p>;
}
