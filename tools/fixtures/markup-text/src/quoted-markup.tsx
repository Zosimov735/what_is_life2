// Fixture: markup held in a single-quoted string is a string, not a text
// node, so it must not be read as markup around it.
export const FALLBACK_MARKUP = '<p>Follow the bright current.</p>';
export const NESTED_SELECTOR = 'section > p';
