/// <reference types="vite/client" />

// The bundler's ambient declarations, which is where `import.meta.env` is
// declared. The shell reads one field of it: whether this is a development
// build, which is what gates the local preview's session handle.
